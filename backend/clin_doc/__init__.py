"""clin_doc — FastAPI backend for the Clinical Documentation Assistant (B1).

Phase 0: runnable skeleton with a health check so the import graph is
exercised. Phase 2 adds the pipeline endpoints (upload/transcribe,
generate-note, suggest-codes, generate-referral, summarize, fhir-export).
"""

__version__ = "0.1.0"
